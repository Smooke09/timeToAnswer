<h1>Site::Welcome#index</h1>
<p>Find me in app/views/site/welcome/index.html.erb</p>

<% if user_signed_in? %>
    <p>Usuario ESTA Logado / <%= current_user.email %></p>
<% else %>
    <p>Usuario NÃO Logado</p>
<% end %>


<% if admin_signed_in? %>
    <p>Admin ESTA Logado / <%= current_admin.email %> </p>
<% else %>
    <p>Admin NÃO Logado</p>
<% end %>
